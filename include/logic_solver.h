#ifndef LOGIC_SOLVER_H
#define LOGIC_SOLVER_H

// Hàm giải thuật chính: tìm bc, bg, nd theo n, dc, dg, ld và thời tiết
void solve(int n, int dc, int dg, int ld, char *weather, int *bc, int *bg, double *nd);

#endif
